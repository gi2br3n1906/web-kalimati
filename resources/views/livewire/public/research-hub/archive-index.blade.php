<main class="research-shell"><header class="research-header"><a href="{{ route('public.gis.map') }}">Peta Desa</a><div><p>Research Hub</p><h1>Arsip Riset KKN</h1></div><a href="{{ route('public.umkm.directory') }}">UMKM</a></header><section class="research-controls"><input type="search" wire:model.live.debounce.300ms="search" placeholder="Cari judul, abstrak, atau penulis"><select wire:model.live="category"><option value="">Semua Kategori</option>@foreach($categories as $value => $label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select><select wire:model.live="cohort"><option value="">Semua Angkatan</option>@foreach($cohorts as $item)<option value="{{ $item }}">{{ $item }}</option>@endforeach</select></section><section class="research-grid">@forelse($files as $file)<article class="research-card"><span>{{ $file->category->label() }}</span><h2>{{ $file->title }}</h2><p>{{ $file->kkn_cohort }} · {{ $file->human_readable_file_size }}</p><p>{{ $file->author_names }}</p><p>{{ $file->abstract }}</p><div><a href="{{ route('public.research.download', $file) }}">Unduh</a>@if($file->is_pdf)<button type="button" wire:click="openViewer({{ $file->id }})">Buka PDF</button>@endif</div></article>@empty<div class="research-empty">Belum ada dokumen yang sesuai.</div>@endforelse</section>@if($viewer && $viewer->is_pdf)<div class="research-modal" role="dialog" aria-modal="true"><div><button type="button" wire:click="closeViewer">Tutup</button><h2>{{ $viewer->title }}</h2><iframe src="{{ route('public.research.preview', $viewer) }}" title="{{ $viewer->title }}"></iframe></div></div>@endif</main>